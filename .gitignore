# petite-cern — gitignore
# Append-only ledger / runtime caches
state/*.log
state/*.jsonl
state/runs/
state/cache/
state/markers/
**/__pycache__/
**/*.pyc

# Editor / OS
.DS_Store
.idea/
.vscode/

# Local env
.env
.env.local

# Build artifacts (none expected — pure hexa)
dist/
build/
